CREATE TABLE Utenti (
    ID_UTENTE      NOT NULL
                   PRIMARY KEY,
    NOME           NOT NULL,
    COGNOME        NOT NULL,
    BIRTH     DATE NOT NULL,
    INDIRIZZO      NOT NULL,
    CARTA          NOT NULL
);
